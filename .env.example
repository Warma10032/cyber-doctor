# 大语言模型支持智谱AI，兼容OpenAI SDK的api和Ollama封装的本地api
# 大语言模型的url地址
LLM_BASE_URL = https://open.bigmodel.cn/api/paas/v4
# LLM_BASE_URL = https://api.deepseek.com/v1
# LLM_BASE_URL = https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_BASE_URL = https://api.siliconflow.cn/v1
# LLM_BASE_URL = https://ark.cn-beijing.volces.com/api/v3
# LLM_BASE_URL = http://127.0.0.1:11434/v1

# 大语言模型的api key
LLM_API_KEY = 
# 大语言模型的名称
MODEL_NAME = glm-4.5-flash
# 可选 glm-4-flash, glm-4-plus, 
# deepseek-chat, deepseek-reasoner, 
# qwen-max, qwen2.5-7b-instruct-1m
# doubao-1-5-pro-32k

# yaml配置文件的选择，web指config/config-web.yaml
PY_ENVIRONMENT = web

# 图片生成模型api地址，目前只支持ZhipuAI
IMAGE_GENERATE_API = 
# 图片生成模型的名称
IMAGE_GENERATE_MODEL = cogview-3-flash

# 多模态（图片理解）大模型api地址，目前只支持ZhipuAI
IMAGE_DESCRIBE_API = 
# 多模态（图片理解）大模型的名称
IMAGE_DESCRIBE_MODEL = glm-4v-flash

# 视频生成模型api地址，目前只支持ZhipuAI
VIDEO_GENERATE_API = 
# 视频生成模型的名称
VIDEO_GENERATE_MODEL = cogvideox-flash

# =========================
# Django 认证服务配置
# =========================

# Django 项目的 Secret Key（务必修改为随机值）
DJANGO_SECRET_KEY = change-me
# 是否开启调试模式（生产环境请保持 false）
DJANGO_DEBUG = false
# 允许访问的主机名，逗号分隔
DJANGO_ALLOWED_HOSTS = 127.0.0.1,localhost
# MySQL 连接参数（默认使用远程库，可按需修改）
DJANGO_DB_NAME = cyber_doctor
DJANGO_DB_USER = root
DJANGO_DB_PASSWORD = change-me
DJANGO_DB_HOST = 114.215.183.142
DJANGO_DB_PORT = 3306
DJANGO_DB_CHARSET = utf8mb4
DJANGO_DB_CONN_MAX_AGE = 60
# 启动 app.py 前是否自动执行 migrate（true/false）
AUTO_MIGRATE = true

# JWT 配置
JWT_SECRET_KEY = change-me-too
JWT_ALGORITHM = HS256
ACCESS_TOKEN_LIFETIME_MINUTES = 60
REFRESH_TOKEN_LIFETIME_DAYS = 7
AUTH_TOKEN_NAMESPACE = cyber_doctor

# Redis 连接地址（未配置则使用本地文件作为降级方案）
REDIS_URL = redis://127.0.0.1:6379/0

# Gradio 前端调用 Django 认证服务的地址
AUTH_SERVER_BASE_URL = http://127.0.0.1:8000

# =========================
# Gradio 服务配置
# =========================
# 监听地址：默认仅本机访问；若要被局域网访问，设为 0.0.0.0
GRADIO_HOST = 127.0.0.1
# 监听端口
GRADIO_PORT = 10032
# 是否开启公网临时分享链接（gradio 内置隧道），默认 false
GRADIO_SHARE = false
